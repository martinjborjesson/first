// Ändra till serverns lokala IP
const IP = "192.168.50.126"

// Låt vara kvar på 5000
const PORT = "5000"

// backend lyssnar på alla port 5000, se "backend/Properties/launchSettings.json"
export const API_IP = `${IP}:${PORT}`